services:
  writegod:
    image: writegod/novel-engine:latest
    container_name: writegod
    env_file:
      - env.txt
    ports:
      - "3000:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads